# ─── Source material: NEVER commit ────────────────────────────────────────────
# The rulebook PDF and the mechanical spec derived from it are not ours to
# distribute. See §17 of the spec for the full reasoning.
*.pdf
last-arc-foundry-system-spec.md

# Compendium content generated from the PDF (§14). The compiled LevelDB packs
# AND the human-readable _source/ trees are both off-limits — _source is the
# more sensitive of the two, not the less. The system ships with EMPTY packs and
# an importer users run against their own licensed copy.
packs/

# Art extracted from the PDF for local sheet development. The sheet is designed
# to render correctly without it; this is an optional local overlay only.
art-local/

# ─── Ordinary noise ───────────────────────────────────────────────────────────
node_modules/
.DS_Store
*.log
# Generated by tools/preview.mjs for offline sheet inspection.
preview*.html
.env
dist/
*.zip
